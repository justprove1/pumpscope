# apps/web

Dashboard (Next.js + TypeScript estricto). **STUB Fase 0.**

Solo consume `apps/api`. No calcula scores, no evalua riesgo y no habla con Solana.
Las pantallas previstas estan en SPEC.md 21; el contrato de datos, en `API.md`.

No hay `node_modules` ni build: la maquina de desarrollo actual no tiene Node instalado.
